server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/brunocastagna98/config-repo-local-electro
          default-label: main
